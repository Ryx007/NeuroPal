# ── OS ───────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Editor ───────────────────────────────────────
.vscode/
.idea/
*.swp

# ── Node (both subprojects) ──────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# ── Env (NEVER commit real secrets) ─────────────
.env
.env.local
.env.*.local
!.env.example

# ── Backend: uploaded user files + audio cache ──
neuropal-backend/storage/

# ── Expo / React Native ─────────────────────────
neuropal-expo-app/.expo/
neuropal-expo-app/.expo-shared/
neuropal-expo-app/web-build/
neuropal-expo-app/dist/

# Native build artefacts — regenerated by `expo prebuild`
neuropal-expo-app/android/
neuropal-expo-app/ios/

# Built binaries
*.apk
*.ipa
*.aab

# Build caches
.cache/
.metro-health-check*
.tsbuildinfo

# ── Reference archive (committed but quiet) ──────
# _archive/ stays in git for historical reference. To exclude entirely,
# uncomment the next line:
# _archive/

# Corrupt pre-pivot git dir (kept locally only)
.git.corrupt.bak/

# Machine-local Claude Code settings
.claude/settings.local.json
