export function serializeDocuments(documents) {
  return documents;
}

export function deserializeDocuments(documents = []) {
  return documents;
}

export function serializeMessages(messages) {
  return messages;
}

export function deserializeMessages(messages = []) {
  return messages;
}
